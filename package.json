{
  "name": "wechatsync",
  "version": "2.0.0",
  "private": true,
  "description": "微信公众号同步助手 - 多平台内容分发工具",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm --filter @wechatsync/extension dev",
    "build": "pnpm -r --workspace-concurrency=1 run build",
    "build:core": "pnpm --filter @wechatsync/core build",
    "build:extension": "pnpm --filter @wechatsync/extension build",
    "build:mcp": "pnpm --filter @wechatsync/mcp-server build",
    "build:cli": "pnpm --filter @wechatsync/cli build",
    "mcp": "pnpm --filter @wechatsync/mcp-server start",
    "test": "pnpm -r run test",
    "lint": "eslint packages --ext .ts,.tsx",
    "typecheck": "pnpm -r run typecheck",
    "clean": "pnpm -r run clean; rimraf node_modules"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@typescript-eslint/eslint-plugin": "^6.13.0",
    "@typescript-eslint/parser": "^6.13.0",
    "eslint": "^8.55.0",
    "prettier": "^3.1.0",
    "rehype-parse": "^9.0.1",
    "rehype-remark": "^10.0.1",
    "remark-gfm": "^4.0.1",
    "remark-stringify": "^11.0.0",
    "turndown": "^7.2.2",
    "turndown-plugin-gfm": "^1.0.2",
    "typescript": "^5.3.0",
    "unified": "^11.0.5"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@modyfi/vite-plugin-yaml": "^1.1.1"
  }
}
